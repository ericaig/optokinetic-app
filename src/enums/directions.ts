export enum DIRECTIONS {
    LEFT = "left",
    UP = "up",
    DOWN = "down",
    RIGHT = "right",
}

export enum SPIN_DIRECTIONS {
    CLOCKWISE = "clockwise",
    ANTICLOCKWISE = "anticlockwise",
    NONE = "none"
}